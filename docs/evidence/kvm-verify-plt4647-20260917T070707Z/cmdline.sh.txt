#!/bin/bash
cd "$(dirname "$0")"
echo "== idle host"; uptime
python3 cmdline.py 1000
echo "== 8 busy loops on 4 vCPUs"
pids=""; for i in 1 2 3 4 5 6 7 8; do (while :; do :; done) & pids="$pids $!"; done
sleep 1; uptime
python3 cmdline.py 1000
kill $pids
wait 2>/dev/null
echo done
